# React Компоненти - Button та Input

## Опис проекту

Це демонстраційний React проект, який показує, як створювати та використовувати переиспользуються компоненти з props та обробниками подій. Проект містить два основних компоненти:

- **Button** - кнопка з налаштовуваним текстом, типом та обробником кліку
- **Input** - поле введення з налаштовуваним placeholder, типом та обробником зміни значення

## Особливості

✅ Функціональні компоненти з використанням React Hooks  
✅ Props та PropTypes для валідації  
✅ Обробники подій (onClick, onChange)  
✅ Стильне оформлення компонентів  
✅ Адаптивний дизайн  
✅ Чистий та читаємий код  

## Технологічний стек

- **React 18** - бібліотека для побудови інтерфейсів
- **Vite** - швидкий build tool
- **PropTypes** - валідація props
- **CSS 3** - для стилізації компонентів

## Структура проекту

```
my-react-app/
├── src/
│   ├── components/
│   │   ├── Button.jsx        # Компонент кнопки
│   │   ├── Button.css        # Стилі кнопки
│   │   ├── Input.jsx         # Компонент поля введення
│   │   └── Input.css         # Стилі поля введення
│   ├── App.jsx              # Головний компонент
│   ├── App.css              # Стилі App
│   ├── main.jsx             # Точка входу
│   └── index.css            # Глобальні стилі
├── public/                   # Статичні файли
├── package.json             # Залежності проекту
├── vite.config.js           # Конфігурація Vite
├── eslint.config.js         # Конфігурація ESLint
└── README.md               # Цей файл
```

## Встановлення та запуск

### Вимоги
- Node.js (версія 14 або вище)
- npm або yarn

### Кроки встановлення

1. **Клонуйте репозиторій** (якщо клонуєте):
```bash
git clone https://github.com/ваш-юзер/my-react-app.git
cd my-react-app
```

2. **Встановіть залежності**:
```bash
npm install
```

3. **Запустіть dev сервер**:
```bash
npm run dev
```

Проект автоматично відкриється в браузері. За замовчуванням він доступний за адресою `http://localhost:5173/`

### Доступні команди

- `npm run dev` - запуск development сервера
- `npm run build` - збірка проекту для production
- `npm run preview` - попередній перегляд production складки
- `npm run lint` - перевірка коду за допомогою ESLint

## Компоненти

### Button компонент

**Файл**: `src/components/Button.jsx`

```jsx
import Button from './components/Button';

<Button 
  text="Клік мене"
  type="button"
  onClick={handleClick}
/>
```

**Props:**
- `text` (string, обов'язковий) - текст кнопки
- `type` (string, опціональний) - тип кнопки (default: "button")
  - `button` - звичайна кнопка
  - `submit` - кнопка для відправки форми
  - `reset` - кнопка для скидання форми
- `onClick` (function, опціональний) - обробник кліку

**Приклад використання:**
```jsx
const handleClick = () => {
  alert('Кнопку натиснуто!');
};

<Button 
  text="Натисніть мене"
  type="button"
  onClick={handleClick}
/>
```

### Input компонент

**Файл**: `src/components/Input.jsx`

```jsx
import Input from './components/Input';

const [value, setValue] = useState('');

<Input 
  placeholder="Введіть текст..."
  type="text"
  value={value}
  onChange={(e) => setValue(e.target.value)}
/>
```

**Props:**
- `placeholder` (string, опціональний) - підказка в полі
- `type` (string, опціональний) - тип поля (default: "text")
  - `text` - текстове поле
  - `password` - приховане поле для паролів
  - `email` - поле для email
  - `number` - числове поле
- `value` (string, опціональний) - значення поля
- `onChange` (function, опціональний) - обробник зміни значення

**Приклад використання:**
```jsx
const [inputText, setInputText] = useState('');

<Input 
  placeholder="Введіть ім'я..."
  type="text"
  value={inputText}
  onChange={(e) => setInputText(e.target.value)}
/>
```

## Функціональність в App.jsx

Головний компонент App.jsx демонструє:

1. **Лічильник** - кнопка, яка збільшує значення на 1
2. **Форма** - введення тексту та пароля з валідацією та показом результатів

## Стилізація

Компоненти мають красивий, сучасний вигляд з:
- Плавні переходи при наведенні
- Адаптивний дизайн для мобільних пристроїв
- Градієнтні фони
- Тіні та закруглені кути
- Dark focus states для доступності

## ESLint конфігурація

Проект використовує ESLint для перевірки якості коду. Для запуску перевірки:

```bash
npm run lint
```

## Розгортання

### Розгортання на Vercel

1. Сформуйте проект:
```bash
npm run build
```

2. Йдіть на [Vercel](https://vercel.com) та помічаєте свій GitHub репозиторій

3. Vercel автоматично розгорне ваш проект

### Розгортання на Netlify

1. Сформуйте проект:
```bash
npm run build
```

2. Йдіть на [Netlify](https://netlify.com) та завантажте папку `dist`

Або з'єднайте ваш GitHub репозиторій та налаштуйте автоматичне розгортання.

## Live демо

🚀 [Дивіться демо](https://your-demo-link.com) (замініть на реальне посилання після розгортання)

## Інструкції для розробника

### Як додати новий компонент

1. Створіть папку компоненту в `src/components/`
2. Створіть файл компоненту (наприклад, `MyComponent.jsx`)
3. Додайте CSS файл для компоненту (`MyComponent.css`)
4. Експортуйте компонент
5. Імпортуйте його в `App.jsx`

### Правила кодування

- Використовуйте функціональні компоненти з Hooks
- Розділяйте логіку на окремі компоненти
- Використовуйте PropTypes для валідації props
- Дотримуйтесь назв у camelCase для функцій та змінних
- Додавайте коментарі для складної логіки

## Сумісність браузерів

Проект підтримує всі сучасні браузери:
- Chrome (остання версія)
- Firefox (остання версія)
- Safari (остання версія)
- Edge (остання версія)
