# 🚀 Деплоймент на Vercel

## Способ 1: Через GitHub (Рекомендується)

### Крок 1: Підготовка GitHub
1. Переконайтесь, що весь код закоммічено та запушено на GitHub
```bash
git add -A
git commit -m "Ready for deployment"
git push origin main
```

### Крок 2: Deployment на Vercel
1. Перейдіть на https://vercel.com
2. Натисніть **"New Project"**
3. Виберіть репозиторій **hw38**
4. Vercel автоматично визначить:
   - **Framework**: Vite
   - **Build Command**: `npm run build`
   - **Output Directory**: `dist`
5. Натисніть **"Deploy"**

**Готово!** Ваш додаток буде розгорнуто на URL типу: `https://your-project.vercel.app`

---

## Способ 2: Через Vercel CLI

### Крок 1: Встановлення
```bash
npm install -g vercel
```

### Крок 2: Вхід
```bash
vercel login
```

### Крок 3: Деплоймент
```bash
cd my-react-app
vercel
```

Слідуйте інструкціям на екрані.

---

## Способ 3: Через GitHub Actions (Автоматичний)

Створіть файл `.github/workflows/deploy.yml`:

```yaml
name: Deploy to Vercel

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: vercel/action@master
        with:
          vercel-token: ${{ secrets.VERCEL_TOKEN }}
          vercel-org-id: ${{ secrets.VERCEL_ORG_ID }}
          vercel-project-id: ${{ secrets.VERCEL_PROJECT_ID }}
```

---

## Перевірка після деплою

1. Відкрийте URL вашого проекту на Vercel
2. Перевірте:
   - ✅ Сторінка завантажується
   - ✅ Компоненти працюють
   - ✅ Input приймає текст
   - ✅ Button рахує клікі
   - ✅ Стилі відображаються правильно

## Проблеми та рішення

### Проблема: "Build failed"
**Рішення**: Перевірте, що `npm run build` працює локально
```bash
npm install --legacy-peer-deps
npm run build
```

### Проблема: "Cannot find module"
**Рішення**: Переконайтесь, що всі залежності в `package.json`

### Проблема: "PORT already in use"
**Рішення**: Це нормально для Vercel, вони автоматично вибирають порт

---

## Оновлення після деплою

Просто пушніть зміни на GitHub, Vercel автоматично перебудує додаток:
```bash
git add -A
git commit -m "Update features"
git push origin main
```

---

**Успішного деплою! 🎉**

